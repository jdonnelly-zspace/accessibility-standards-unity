# Git Attributes - Unity + Node.js Hybrid Repository

# Auto-detect text files and perform LF normalization
* text=auto

# Unity binary files - no diffing or merging
*.meta -diff -merge
*.unity -diff -merge
*.prefab -diff -merge
*.asset -diff -merge
*.mat -diff -merge
*.controller -diff -merge
*.anim -diff -merge
*.fbx -diff -merge
*.unitypackage -diff -merge

# C# files - normalize line endings to LF
*.cs text eol=lf diff=csharp
*.csproj text eol=crlf
*.sln text eol=crlf

# JavaScript and JSON - normalize to LF
*.js text eol=lf
*.mjs text eol=lf
*.json text eol=lf

# Documentation - normalize to LF
*.md text eol=lf
*.txt text eol=lf

# YAML files - normalize to LF
*.yml text eol=lf
*.yaml text eol=lf

# Shell scripts - normalize to LF
*.sh text eol=lf
*.bash text eol=lf

# Windows batch files - CRLF
*.bat text eol=crlf
*.cmd text eol=crlf

# Binary files - explicit binary handling
*.png binary
*.jpg binary
*.jpeg binary
*.gif binary
*.bmp binary
*.tga binary
*.psd binary
*.ai binary
*.wav binary
*.mp3 binary
*.ogg binary
*.dll binary
*.so binary
*.dylib binary
*.exe binary
*.zip binary
*.tar binary
*.gz binary

# Git LFS (for future use with large binary assets)
*.png filter=lfs diff=lfs merge=lfs -text
*.jpg filter=lfs diff=lfs merge=lfs -text
*.jpeg filter=lfs diff=lfs merge=lfs -text
*.fbx filter=lfs diff=lfs merge=lfs -text
*.unitypackage filter=lfs diff=lfs merge=lfs -text
*.pdf filter=lfs diff=lfs merge=lfs -text
